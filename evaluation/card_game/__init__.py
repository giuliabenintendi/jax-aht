"""Card-game-specific evaluation drivers and shared helpers.

Maintained outputs:
  - `plot_sp_xp.py` for the SP/XP summary figure
  - `attention_numbers.py` for visual attention inspection
  - `action_distributions.py` for per-agent action-distribution artifacts

Shared loading and checkpoint selection live in `_card_game_utils.py`.
"""
